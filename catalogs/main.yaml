description: 'Master Data Catalog'
sources:

  hydrology:
    args:
      path: "{{CATALOG_DIR}}/hydrology.yaml"
    description: 'Hydrology Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}

  atmosphere:
    args:
      path: "{{CATALOG_DIR}}/atmosphere.yaml"
    description: 'Atmospheric Science Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}
    
  geography:
    args:
      path: "{{CATALOG_DIR}}/geography.yaml"
    description: 'Geography Science Dataset Catalog'
    driver: intake.catalog.local.YAMLFileCatalog
    metadata: {}    
    
    
